@model IEnumerable<WebApplicationMVCBatch3.Models.Location>

@{
    ViewData["Title"] = "GetLoc";
}

<h1>GetLoc</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Emp.EName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Emp.EName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EmpID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EmpID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EmpID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
